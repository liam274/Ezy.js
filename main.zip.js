"use strict"; import *as utils from "./utils.js"; export const log = console.log, $ = document.querySelector.bind(document), $$ = document.createElement.bind(document), error = console.error.bind(console), warn = console.warn.bind(console); function removePrefix(str, prefix) { return str.startsWith(prefix) ? str.slice(prefix.length) : str } export const keyword = new Set(["type", "component", "tag", "events", "style", "varAs", "listener", "times", "title", "if", "content", "inherit", "validate", "expire", "text", "forEach", "innerHTML", "config", "data"]), errors = { STRUCTURE_ERROR: 1, ASVAR_REWRITE_ERROR: 2, FORMAT_ERROR: 3, TEMPLATE_STRUCTURE_ERROR: 4, VALUE_ERROR: 5, EVAL_ERROR: 6, CLASSIFY_ERROR: 7, PIPE_ERROR: 8, RENDER_ERROR: 9, PHARSING_ERROR: 10, ID_ERROR: 11 }; export const dictionary = { time: ["i", "index", "renderIndex"], key: ["key"], item: ["item", "value"] }; class unknownVariableError extends Error { constructor(...data) { super(...data); this.name = "unknownVariableError" } getName() { return "unknownVariableError" } }; export const body = document.body; export const Ezy = { plugins: [], add(plugin) { this.plugins.push(plugin) }, render() { }, isInt(data) { return Number.isInteger(Number(data)) }, isAlphabet(data) { return utils.ALPHABET_REGEX.test(data) }, isEmail(data) { return utils.EMAIL_REGEX.test(data) }, isDate(data) { return utils.DATE_REGEX.test(data) }, validatePipe(obj, data, traceback) { if (!data.pipe.receive || typeof data.pipe.receive !== "object") { Ezy.formatError(`Error when rendering,expected object[string,function],found ${data.pipe.receive},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Pipe Error"); return obj.set(errors.PIPE_ERROR) } for (const i in data.pipe.receive) { const sobj = data.pipe.receive[i]; if (typeof sobj.func !== "function") { Ezy.formatError(`Error when piping,expected data.pipe.receive.*.func as function,found ${typeof sobj.func},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Structure Error"); return obj.set(errors.STRUCTURE_ERROR) } if (!Array.isArray(sobj.data)) { Ezy.formatError(`Error when piping,expected data.pipe.receive.*.data as array,found ${typeof sobj.data},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Structure Error"); return obj.set(errors.STRUCTURE_ERROR) } } if (!data.pipe.name) { Ezy.formatError(`Error when piping,expected data.pipe.name,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Structure Error"); return obj.set(errors.STRUCTURE_ERROR) } }, validateComponentIf(obj, item, traceback) { if (item) { if (!obj.varage[item]) { Ezy.formatError(`render.varage[component.if]not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error"); return obj.set(errors.VALUE_ERROR) } if (typeof obj.varage[item] !== "function") { Ezy.formatError(`expected render.varage[component.if]as function,found ${typeof obj.varage[item]},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error"); return obj.set(errors.VALUE_ERROR) } return !obj.varage[item]() } }, validateValidation(obj, el, validate, traceback) { if (validate && typeof validate === "string") { if (Ezy[validate]) { if (typeof Ezy[validate] === "function") { el.addEventListener("input", function (e) { if (Ezy[validate](e.target.value)) { el.classList.add("valid"); el.classList.remove("invalid") } else { el.classList.remove("valid"); el.classList.add("invalid") } }) } else { Ezy.formatError(`Error when rendering,expected component.validate in Ezy as function,found ${typeof Ezy[validate]},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return obj.set(errors.RENDER_ERROR) } } else { Ezy.formatError(`Error when rendering,Ezy[component.validate]not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return obj.set(errors.RENDER_ERROR) } } }, flat(data) { const result = []; for (const i of data) { if (Array.isArray(i)) { result.push(...this.flat(i)) } else { result.push(i) } } return result }, alert(data) { const barrier = $$("div"); barrier.classList.add("alert-barrier"); const back = $$("div"); back.classList.add("alert-back"); barrier.appendChild(back); const title = $$("div"); title.classList.add("alert-title"); title.innerHTML = data.title; back.appendChild(title); const content = $$("div"); content.innerHTML = data.content; content.classList.add("alert-content"); back.appendChild(content); const confirm = $$("button"); confirm.innerHTML = "OK"; confirm.classList.add("alert-button"); confirm.addEventListener("click", () => { utils.removeChild(barrier); body.removeChild(barrier); barrier.remove() }); back.appendChild(confirm); body.appendChild(barrier) }, formatError(message, level, error) { error(`[ezy.js]${errorLevels[level]}:${error.toLocaleUpperCase()}:${message}`) } }; export const errorLevels = Object.freeze({ CRITICAL_ERROR: 2, MAJOR_ERROR: 1, MINOR_ERROR: 0 }); Ezy.errors = ["MINOR ERROR", "MAJOR ERROR", "CRITICAL ERROR"]; export const routeGuard = { builtin: new Set([]), guards: [] }; routeGuard.guards.push(function (href) { return { allow: routeGuard.builtin.has(href) } }); Ezy.navigate = function (href) { let full = true; for (const guard of routeGuard.guards) { const result = guard(href); if (!result.allow) { if (result.href) { location.href = result.href } full = false } } if (full) { location.href = href } }; const varage = {}, vars = new Set(), ARGS = { classList: (data) => Array.from(data), innerHTML: (data) => data, id: (data) => data, style: (data) => { const result = {}; for (const i in data) { if (Ezy.isInt(i)) { const got = utils.array2camel(data[i].split("-")); result[got] = data[got] } } return { ...result } }, title: (data) => data, events: (data) => { return { ...data } } }; export const MAXWAIT = 60000, HTTP_NOT_FOUND = 404, HTTP_TIMEOUT = 408, SECOND = 1000; export class render { #varage = {}; #frameID = undefined; constructor(el, data, maxWait = MAXWAIT, namespace = {}) { this.maxWait = maxWait; this.data = data; this.mains = []; this.pipes = {}; this.vdom = { children: [], dataset: {} }; if (!data) { this.set(errors.STRUCTURE_ERROR); this.loadPage = this.loadingPage("[ezy.js] CRITICAL ERROR: Structure Error: Data structure missing.", HTTP_NOT_FOUND, this.maxWait); return this } if (data.classify) { this.classify = data.classify } if (!this.data.main) { this.set(errors.STRUCTURE_ERROR); this.loadPage = this.loadingPage("[ezy.js] CRITICAL ERROR: Structure Error: Data structure attribute \"main\" missing.", HTTP_NOT_FOUND, this.maxWait, "Resource page.data.main not found"); return this } this.config = data.config || {}; this.namespace = namespace; if (typeof el === "string") { this.mainEl = $(el) } else { this.mainEl = el } { for (const i in ARGS) { this.vdom[i] = ARGS[i](this.mainEl[i]) } this.vdom.tag = this.mainEl.tagName; this.vdom.dataset = { ...this.vdom.dataset, ...this.mainEl.dataset } } this.original = this.mainEl.innerHTML; this.el = document.createDocumentFragment(); this.reRender(); if (this.statusCode !== 0) { return this } } reRender() { if (this.config && !this.config.keepConsole) { console.clear() } this.historyRender = +new Date(); if (this.loadPage) { this.clearLoading() } if (!this.data) { this.set(errors.STRUCTURE_ERROR); this.loadPage = this.loadingPage("[ezy.js] CRITICAL ERROR: Structure Error: Data structure missing.", HTTP_NOT_FOUND, this.maxWait); return } this.loadPage = this.loadingPage("[ezy.js] CRITICAL ERROR: Timeout Error: ", HTTP_TIMEOUT, this.maxWait, "Page render timeout"); this.clear(); this.#varage = { ...varage, ...(this.data.data || {}) }; this.statusCode = 0; this.systemPlot = { time: 0 }; this.mains.length = 0; for (const key in this.pipes) { delete this.pipes[key] } this.interval = false; if (this.oldTimeout) { clearTimeout(this.oldTimeout) } this.oldTimeout = setTimeout(() => { this.interval = true; this.loop() }, SECOND - ((this.historyRender) % SECOND)); this.main(); if (this.statusCode !== 0) { return } if (this.el) { this.mainEl.appendChild(this.el) } this.clearLoading(); log(`[ezy.js]Debug Message::Render consumed ${new Date() - this.historyRender}ms`) } main() { this.statusCode = 0; if (this.data.onStart) { this.preRender(this.data.onStart) } else { warn("[ezy.js] MAJOR SUGGESTION: : Suggest adding onStart function list to handle preprocess") } for (const i of Ezy.plugins) { i.onStart?.(this.data) } if (!this.data.main) { this.set(errors.STRUCTURE_ERROR); Ezy.formatError("Data structure incomplete.", errorLevels.CRITICAL_ERROR, "Structure Error"); this.loadPage = this.loadingPage("", HTTP_NOT_FOUND, this.maxWait, "Resouce page.data.main not found"); return } this.mainRender(this.data.main); if (this.statusCode !== 0) { return } log(`[ezy.js]Render Program exits ${this.statusCode === 0 ? "" : "un"}successfully.Status Code:${this.statusCode}`); if (this.data.onLoad) { for (const i of this.data.onLoad) { i(this.data) } } else { warn("[ezy.js] MINOR SUGGESSION: : Suggest adding onLoad function list to handle onLoad process") } for (const i of Ezy.plugins) { i.onLoad?.(this.data) } if (this.statusCode !== 0) { return } } loop() { for (const i of this.mains) { i.func(i.obj, i.el) } cancelAnimationFrame(this.#frameID); if (this.interval) { this.#frameID = requestAnimationFrame(this.loop.bind(this)) } } pipe2(sender, receiver, data) { if (!(sender in this.pipes)) { Ezy.formatError(`Error when piping,trying to send message from ${sender},not found in this.pipes`, errorLevels.CRITICAL_ERROR, "Pipe Error"); return this.set(errors.PIPE_ERROR) } if (!(receiver in this.pipes)) { Ezy.formatError(`Error when piping,trying to send message to ${receiver},not found in this.pipes`, errorLevels.CRITICAL_ERROR, "Pipe Error"); return this.set(errors.PIPE_ERROR) } if (!(sender in this.pipes[receiver].receive)) { Ezy.formatError(`Error when piping,try to receive message from ${sender},not found in this.pipes.${receiver}.receive`, errorLevels.CRITICAL_ERROR, "Pipe Error"); return this.set(errors.PIPE_ERROR) } const obj = this.pipes[receiver].receive[sender]; obj.func(data, ...obj.data) } edit(key, data) { this.#varage[key] = data } read(key) { if (key in this.#varage) { return this.#varage[key] } else { throw new unknownVariableError(`[ezy.js]Critical Error:Variable Error:Variable"${key}"not found`); } } preRender(data) { for (const i of data.funcs) { i(this.data); } } removeVdom(data, vdom = this.vdom) { for (let i = vdom.children.length; i > 0; i--) { if (vdom.children[i - 1] === data) { delete vdom.children[i - 1]; return true; } if (this.removeVdom(data, vdom.children[i - 1])) { return true; } } return false; } editVdom(data, func, vdom = this.vdom) { for (let i = vdom.children.length; i > 0; i--) { if (vdom.children[i - 1] === data) { func(vdom.children, i - 1); return true; } if (this.editVdom(data, func, vdom.children[i - 1])) { return true; } } return false; } sectionRender = (sectionData, parentElement, sectionName, title, createElement) => { const traceback = `page ${title}->${sectionName}`; if (!sectionData) { Ezy.formatError(`function found first parameter in ${sectionData},expected object,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error"); return this.set(errors.VALUE_ERROR); } const todo = document.createDocumentFragment(), vdom = []; this.systemPlot.time = 0; for (const i in sectionData) { const item = sectionData[i]; if (Ezy.validateComponentIf(this, item.if, traceback)) { continue; } if (this.statusCode !== 0) { return; } const temp = createElement(i, item, i.config || {}); if (this.statusCode !== 0) { return; } if (!(temp && temp.el && temp.obj)) { Ezy.formatError(`argument-function"createElement"return unexpected value,expected{el:Node(or NodeLike object),obj:vdom},in page ${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error"); return this.set(errors.VALUE_ERROR); } const { el, obj } = temp; if (!(el instanceof Node)) { Ezy.formatError(`argument-function"createElement"return unexpected value,expected{el:Node(or NodeLike object),obj:vdom},in page ${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error"); return this.set(errors.VALUE_ERROR); } todo.appendChild(el); this.systemPlot.time++; vdom.push(...obj); } parentElement.appendChild(todo); return vdom; }; contentRender = (_, i, config) => { const title = this.data.main.title, traceback = `page ${title}->content`, vdom = []; if (typeof i === "string") { if (!this.classify) { Ezy.formatError(`Error when trying to use classify component without classify dictionary,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Classify Error"); return this.set(errors.CLASSIFY_ERROR); } if (!this.classify[i]) { Ezy.formatError(`Error when trying to use classify component"${i}"without definition,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Classify Error"); return this.set(errors.CLASSIFY_ERROR); } i = this.classify[i]; } const todo = document.createDocumentFragment(); if (i.forEach) { if (this.#varage[i.forEach] === undefined) { Ezy.formatError(`Error when rendering,expected forEach variable,not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } const obj = this.#varage[i.forEach]; if (!(obj && typeof obj === "object")) { Ezy.formatError(`Error when rendering,expected object as forEach variable value,found ${obj},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } let first = -1; for (const k in obj) { first++; const card = $$(i.tag || config.tag || "div"), temp = { children: [], dataset: {} }; card.classList.add(...(i.type || []), ...(config.type || [])); if (i.expire) { setTimeout((function () { card.innerHTML = ""; card.parentNode.removeChild(card); if (i.pipe) { delete this.pipes[i.pipe.name]; } this.removeVdom(temp); setTimeout(() => { i.expire.expired?.(); }); }).bind(this), i.expire.date - this.historyRender); } Ezy.validateValidation(this, card, i.validate, traceback); if (this.statusCode !== 0) { return; } if (i.main) { if (typeof i.main !== "function") { Ezy.formatError(`Error when rendering,expected component.main attribute as function,found ${typeof i.main},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } this.mains.push({ el: card, obj: i, func: i.main }); } if (i.pipe) { Ezy.validatePipe(this, i.pipe, traceback); if (this.statusCode !== 0) { return; } this.pipes[i.pipe.name] = i.pipe; } const replacement = { ...this.systemPlot, ...(i.inherit || {}), key: k, item: obj[k] }; if (i.data) { for (const k in i.data) { card.setAttribute(`data-${utils.camel2array(k).join("-")}`, this.preCompileStr(i.data[k], traceback, replacement)); } } this.beforePlugComponent(card, traceback); if (this.statusCode !== 0) { return; } if (first === 0 && i.varAs) { this.asVar(card, i.varAs, traceback); if (this.statusCode !== 0) { return; } } if (i.text) { card.title = this.preCompileStr(i.text, traceback, replacement); } todo.appendChild(card); this.plugComponent(card, traceback); if (this.statusCode !== 0) { return; } utils.applyStyles(card, i.style); for (const j in (i.events || {})) { this.addListener(j, i, card, traceback); if (this.statusCode !== 0) { return; } } card.innerHTML += this.preCompileStr((i.content || ""), traceback, replacement); if (this.statusCode !== 0) { return; } for (const j in i) { if (keyword.has(j)) { continue; } card.setAttribute(j, this.preCompileStr(i[j], traceback, replacement)); temp[j] = card[j]; } temp.children.push(...this.pushComponent(i, card, traceback, replacement)); if (this.statusCode !== 0) { return; } { for (const i in ARGS) { temp[i] = ARGS[i](card[i]); } temp.tag = card.tagName; temp.dataset = { ...temp.dataset, ...card.dataset }; }; vdom.push(temp); } } else { for (let k = 0; k < (i.times || 1); k++) { const card = $$(i.tag || config.tag || "div"), temp = { children: [], dataset: {} }; card.classList.add(...(i.type || []), ...(config.type || [])); if (i.expire) { setTimeout((function () { card.innerHTML = ""; card.parentNode.removeChild(card); if (i.pipe) { delete this.pipes[i.pipe.name]; } this.removeVdom(temp); setTimeout(() => { i.expire.expired?.(); }); }).bind(this), i.expire.date - this.historyRender); } Ezy.validateValidation(this, card, i.validate, traceback); if (this.statusCode !== 0) { return; } if (i.main) { if (typeof i.main !== "function") { Ezy.formatError(`Error when rendering,expected component.main attribute as function,found ${typeof i.main},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } this.mains.push({ el: card, obj: i, func: i.main }); } if (i.pipe) { Ezy.validatePipe(this, i.pipe, traceback); if (this.statusCode !== 0) { return; } this.pipes[i.pipe.name] = i.pipe; } if (i.data) { for (const k in i.data) { card.setAttribute(`data-${utils.camel2array(k).join("-")}`, this.preCompileStr(i.data[k], traceback, i.inherit || {})); } } this.beforePlugComponent(card, traceback); if (this.statusCode !== 0) { return; } if (k === 0 && i.varAs) { this.asVar(card, i.varAs, traceback); if (this.statusCode !== 0) { return; } } if (i.text) { card.title = this.preCompileStr(i.text, traceback, i.inherit || {}); } todo.appendChild(card); this.plugComponent(card, traceback); if (this.statusCode !== 0) { return; } utils.applyStyles(card, i.style); for (const j in (i.events || {})) { this.addListener(j, i, card, traceback); if (this.statusCode !== 0) { return; } } card.innerHTML += this.preCompileStr((i.content || ""), traceback, i.inherit || {}); if (this.statusCode !== 0) { return; } for (const j in i) { if (keyword.has(j)) { continue; } card.setAttribute(j, this.preCompileStr(i[j], traceback, i.inherit)); temp[j] = card[j]; } temp.children.push(...this.pushComponent(i, card, traceback, i.inherit)); if (this.statusCode !== 0) { return; } { for (const i in ARGS) { temp[i] = ARGS[i](card[i]); } temp.tag = card.tagName; temp.dataset = { ...temp.dataset, ...card.dataset }; }; vdom.push(temp); } } return { el: todo, obj: vdom }; }; mainRender(pageData) { this.vdom.children.push(...this.sectionRender(pageData, this.el, pageData.name || "", pageData.title || "", this.contentRender)); } set(code) { this.statusCode = code; } evaluateExpression(expr, traceback, extraScope) { expr = expr.trim(); if (!expr) { return ""; } let last = false, double = false, exist = false; for (const i of expr) { if (i === "|") { exist = true; if (last) { double = true; break; } last = true; continue; } last = false; double = false; } if (!double && exist) { const varName = []; let first = true, result = undefined; for (const char of expr) { if (char === "|") { let name = varName.join("").trim(); if (first) { if (extraScope[name]) { result = extraScope[name]; } else { Ezy.formatError(`Error when trying to access variable ${name},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error"); return this.set(errors.PHARSING_ERROR); } first = false; } else { name = name.split(":"); if (extraScope[name[0]]) { if (typeof extraScope[name[0]] === "function") { result = this.evaluateExpression(`${name[0]}(result,${name.filter((_, i) => i).join(", ")})`, traceback, { ...extraScope, result }); } else { Ezy.formatError(`Error when parsing,expected filter as function,found ${typeof extraScope[name[0]]},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error"); return this.set(error.PHARSING_ERROR); } } else { Ezy.formatError(`Error when trying to access variable ${name[0]},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error"); return this.set(errors.PHARSING_ERROR); } } varName.length = 0; } else { varName.push(char); } } let name = varName.join("").trim(); if (first) { if (extraScope[name]) { result = extraScope[name]; } else { Ezy.formatError(`Error when trying to access variable ${name},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error"); return this.set(errors.PHARSING_ERROR); } first = false; } else { name = name.split(":"); if (extraScope[name[0]]) { if (typeof extraScope[name[0]] === "function") { result = this.evaluateExpression(`${name[0]}(result,${name.filter((_, i) => i).join(", ")})`, traceback, { ...extraScope, result }); } else { Ezy.formatError(`Error when parsing,expected filter as function,found ${typeof extraScope[name[0]]},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error"); return this.set(errors.PHARSING_ERROR); } } else { Ezy.formatError(`Error when trying to access variable ${name[0]},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error"); return this.set(errors.PHARSING_ERROR); } } return result; } const keys = Object.keys(extraScope); const values = keys.map(k => extraScope[k]); try { const fn = new Function(...keys, `return(${expr})`); const result = fn(...values); return result === undefined ? "" : String(result); } catch (e) { if (e instanceof ReferenceError) { warn(`[ezy.js]Warning:Variable not defined in"${expr}"at ${traceback}`); return ""; } Ezy.formatError(`Failed to evaluate"${expr}"in ${traceback}`, errorLevels.CRITICAL_ERROR, "Eval Error"); error(e); this.set(errors.EVAL_ERROR); return ""; } } preCompileStr(data, traceback, replacement = {}, pipeData = {}) { const result = []; let skip = false, startVar = false, longVar = false, stop = false, doubleStop = false, varName = []; replacement = { ...this.systemPlot, ...replacement }; const newReplacement = {}; const _varage = { ...this.#varage, ...pipeData }; for (const i in replacement) { for (const t of (dictionary[i] || [i])) { newReplacement[t] = replacement[i]; } } for (const i in newReplacement) { data = data.replaceAll(`{{${i}}}`, newReplacement[i]); } for (const i of data) { if (skip) { if (varName.length) { varName.push(i); } else { result.push(i); } skip = false; continue; } if ((stop && !longVar) || (doubleStop && longVar)) { varName = varName.join("").trim(); const _var = stop ? _varage : newReplacement; if (_var[varName]) { if ((typeof _var[varName]) === "function") { result.push(String(_var[varName]())); } else { result.push(String(_var[varName])); } } else { try { result.push(String(this.evaluateExpression(varName, traceback, _var))); } catch (e) { Ezy.formatError(`Error when trying to eval expression ${varName},as below,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Eval Error"); error(e); return this.set(errors.EVAL_ERROR); } } stop = false; doubleStop = false; startVar = false; longVar = false; varName = []; } if (i === "\\") { skip = true; continue; } if (i === "{") { if (varName.length) { Ezy.formatError(`Error when formatting string,unexpected character"{"within value,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } if (longVar) { Ezy.formatError(`Error when formatting string,unexpected character"{"with two openings already,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } if (startVar) { startVar = false; longVar = true; } else { startVar = true; } continue; } if (i === "}") { if (!(startVar || longVar)) { Ezy.formatError(`Error when formatting string,unexpected ending without any opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } if (stop) { if (startVar) { Ezy.formatError(`Error when formatting string,unexpected double ending with single opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } stop = false; doubleStop = true; continue; } if (startVar) { startVar = false; if (doubleStop) { Ezy.formatError(`Error when formatting string,unexpected ending with single opening already,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } stop = true; } if (longVar) { if (stop) { doubleStop = true; stop = false; } else { stop = true; } } continue; } if (startVar || longVar) { varName.push(i); continue; } result.push(i); } if (longVar && !doubleStop) { Ezy.formatError(`Error when formatting string,unclosed double opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } if (startVar && !stop) { Ezy.formatError(`Error when formatting string,unclosed single opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } if (skip) { Ezy.formatError(`Error when formatting string,expected any character after\\in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error"); return this.set(errors.FORMAT_ERROR); } varName = varName.join("").trim(); if (stop || doubleStop) { const _var = stop ? _varage : newReplacement; if (_var[varName]) { if ((typeof _var[varName]) === "function") { result.push(String(_var[varName]())); } else { result.push(String(_var[varName])); } } else { try { result.push(String(this.evaluateExpression(varName, traceback, _var))); } catch (e) { Ezy.formatError(`Error when trying to eval expression ${varName},as below,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Eval Error"); error(e); return this.set(errors.EVAL_ERROR); } } } return result.join(""); } pushComponent(i, parentNode, traceback, replacement = {}) { const own = { time: 0 }, todo = document.createDocumentFragment(); const config = i.config || {}, vdom = []; for (let j of (i.component || [])) { if (typeof j === "string") { if (!this.classify) { Ezy.formatError(`Error when trying to use classify component without classify dictionary,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Classify Error"); return this.set(errors.CLASSIFY_ERROR); } if (!this.classify[j]) { Ezy.formatError(`Error when trying to use classify component"${j}"without definition,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Classify Error"); return this.set(errors.CLASSIFY_ERROR); } j = this.classify[j]; } if (Ezy.validateComponentIf(this, j.if, traceback)) { continue; } if (this.statusCode !== 0) { return; } if (j.forEach) { if (this.#varage[j.forEach] === undefined) { Ezy.formatError(`Error when rendering,expected forEach variable,not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } const obj = this.#varage[j.forEach]; if (!(obj && typeof obj === "object")) { Ezy.formatError(`Error when rendering,expected object as forEach variable value,found ${obj},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } let first = -1; for (const k in obj) { first++; const el = $$(j.tag || config.tag || "div"), temp = { children: [], dataset: {} }; el.classList.add(...(j.type || []), ...(config.type || [])); utils.applyStyles(el, j.style); const myTraceback = traceback + `->${el.tagName}${el.id ? "#" + el.id : ""}.${[...el.classList].join(".")}`; for (const evt in j.events) { this.addListener(evt, j, el, myTraceback); if (this.statusCode !== 0) { return; } } const replace = { ...replacement, ...j.inherit, key: k, item: obj[k], ...own }; for (const parm in j) { if (keyword.has(parm)) { continue; } el.setAttribute(parm, this.preCompileStr(j[parm], myTraceback, replace)); temp[parm] = el[parm]; } el.innerHTML = this.preCompileStr((j.content || ""), myTraceback, replace); if (this.statusCode !== 0) { return; } if (j.expire) { setTimeout((function () { el.innerHTML = ""; el.parentNode.removeChild(el); if (j.pipe) { delete this.pipes[j.pipe.name]; } this.removeVdom(temp); setTimeout(() => { j.expire.expired?.(); }); }).bind(this), j.expire.date - this.historyRender); } Ezy.validateValidation(this, el, j.validate, traceback); if (this.statusCode !== 0) { return; } if (j.main) { if (typeof j.main !== "function") { Ezy.formatError(`Error when rendering,expected component.main attribute as function,found ${typeof j.main},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } this.mains.push({ el, obj: j, func: j.main }); } if (j.pipe) { Ezy.validatePipe(this, j.pipe, traceback); if (this.statusCode !== 0) { return; } this.pipes[j.pipe.name] = j.pipe; } if (j.text) { el.title = this.preCompileStr(j.text, myTraceback, replace); } if (j.data) { for (const k in j.data) { el.setAttribute(`data-${utils.camel2array(k).join("-")}`, this.preCompileStr(j.data[k], myTraceback, replace)); } } this.beforePlugComponent(el, myTraceback); if (this.statusCode !== 0) { return; } if (first === 0) { this.asVar(el, j.varAs, myTraceback); if (this.statusCode !== 0) { return; } } todo.appendChild(el); this.plugComponent(el, myTraceback); if (this.statusCode !== 0) { return; } temp.children.push(...this.pushComponent(j, el, myTraceback, replace)); if (this.statusCode !== 0) { return; } { for (const i in ARGS) { temp[i] = ARGS[i](el[i]); } temp.tag = el.tagName; temp.dataset = { ...temp.dataset, ...el.dataset }; }; vdom.push(temp); } } else { for (let k = 0; k < (j.times || 1); k++) { const el = $$(j.tag || config.tag || "div"), temp = { children: [], dataset: {} }; el.classList.add(...(j.type || []), ...(config.type || [])); utils.applyStyles(el, j.style); const myTraceback = traceback + `->${el.tagName}${el.id ? "#" + el.id : ""}.${[...el.classList].join(".")}`; for (const evt in j.events) { this.addListener(evt, j, el, myTraceback); if (this.statusCode !== 0) { return; } } for (const parm in j) { if (keyword.has(parm)) { continue; } el.setAttribute(parm, this.preCompileStr(j[parm], myTraceback, { ...replacement, ...j.inherit, ...own })); temp[parm] = el[parm]; } el.innerHTML = this.preCompileStr((j.content || ""), myTraceback, { ...replacement, ...j.inherit, ...own }); if (this.statusCode !== 0) { return; } if (j.expire) { setTimeout((function () { el.innerHTML = ""; if (el.parentNode && el.parentNode.contains(el)) { el.parentNode.removeChild(el); } if (j.pipe) { delete this.pipes[j.pipe.name]; } this.removeVdom(temp); setTimeout(() => { j.expire.expired?.(); }); }).bind(this), j.expire.date - this.historyRender); } Ezy.validateValidation(this, el, j.validate, traceback); if (this.statusCode !== 0) { return; } if (j.main) { if (typeof j.main !== "function") { Ezy.formatError(`Error when rendering,expected component.main attribute as function,found ${typeof j.main},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error"); return this.set(errors.RENDER_ERROR); } this.mains.push({ el, obj: j, func: j.main }); } if (j.pipe) { Ezy.validatePipe(this, j.pipe, traceback); if (this.statusCode !== 0) { return; } this.pipes[j.pipe.name] = j.pipe; } if (j.text) { el.title = this.preCompileStr(j.text, myTraceback, { ...replacement, ...j.inherit, ...own }); } if (j.data) { for (const k in j.data) { el.setAttribute(`data-${utils.camel2array(k).join("-")}`, this.preCompileStr(j.data[k], myTraceback, { ...replacement, ...j.inherit, ...own })); } } this.beforePlugComponent(el, myTraceback); if (this.statusCode !== 0) { return; } if (k === 0) { this.asVar(el, j.varAs, myTraceback); if (this.statusCode !== 0) { return; } } todo.appendChild(el); this.plugComponent(el, myTraceback); if (this.statusCode !== 0) { return; } temp.children.push(...this.pushComponent(j, el, myTraceback, { ...replacement, ...j.inherit, ...own })); if (this.statusCode !== 0) { return; } { for (const i in ARGS) { temp[i] = ARGS[i](el[i]); } temp.tag = el.tagName; temp.dataset = { ...temp.dataset, ...el.dataset }; }; vdom.push(temp); } } own.time++; parentNode.appendChild(todo); todo.replaceChildren(); } return vdom; } asVar(el, varAs, traceback) { if (varAs) { if (vars.has(varAs)) { Ezy.formatError(`when rendering ${el.tagName}.${[...el.classList].join(".")},id collide to"${varAs}",in ${traceback}`, errorLevels.CRITICAL_ERROR, "ID Error"); return this.set(errors.ID_ERROR); } vars.add(varAs); el.id = varAs; } } clear() { for (const i of vars) { window[i].id = ""; } utils.removeChild(this.mainEl); this.mainEl.innerHTML = this.original; this.vdom = { children: [], dataset: {} }; vars.clear(); } plugComponent(el, traceback) { for (const i of Ezy.plugins) { i.onComponentLoad?.(this, el, traceback); if (this.statusCode !== 0) { return; } } } beforePlugComponent(el, traceback) { for (const i of Ezy.plugins) { i.beforeComponentLoad?.(this, el, traceback); if (this.statusCode !== 0) { return; } } } addListener(j, i, el, traceback) { const obj = i.events[j], { listener } = obj; if (!listener) { Ezy.formatError(`Expected"listener"attribute in second parameter->events[first parameter],in ${traceback}`, errorLevels.MINOR_ERROR, "Value Error"); return this.set(errors.VALUE_ERROR); } if (obj.preventDefault) { el.addEventListener(removePrefix(j, "on"), function (e) { e.preventDefault(); for (const i of listener) { i(e); } }); } else { el.addEventListener(removePrefix(j, "on"), function (e) { for (const i of listener) { i(e); } }); } } loadingPage(msg, errorCode, guillotine = MAXWAIT, reason = "Resource page.data not found", parentNode = body) { const pot = $$("div"); pot.classList.add("flex", "horizontal-mid", "vertical-mid", "bg-white"); pot.style.width = "100%"; pot.style.height = "100%"; const temp = $$("img"); temp.src = "./assets/loading.svg"; pot.appendChild(temp); parentNode.appendChild(pot); return { obj: pot, parent: parentNode, id: setTimeout(() => { parentNode.removeChild(pot); this.errorPage(msg, errorCode, reason, parentNode); }, guillotine) }; } errorPage(msg, errorCode, reason, parentNode = body) { error(msg); const pot = $$("div"); pot.classList.add("flex", "bg-white", "horizontal-mid", "vertical-mid"); pot.style.width = "100%"; pot.style.height = "100%"; const div = $$("div"); div.innerHTML = errorCode || "404"; div.style.fontSize = "20px"; div.style.border = "1px solid black"; div.style.borderWidth = "0 1px 0 0"; div.style.padding = "0 50px"; div.style.color = "black"; div.style.width = "auto"; div.style.height = "auto"; const another = $$("div"); another.innerHTML = reason; another.style.fontSize = "18px"; another.style.color = "black"; another.style.padding = "0 50px"; another.style.width = "auto"; another.style.height = "auto"; pot.appendChild(div); pot.appendChild(another); parentNode.appendChild(pot); } clearLoading() { if (!this.loadPage) { return; } clearTimeout(this.loadPage.id); if (this.loadPage.parent.contains(this.loadPage.obj)) { this.loadPage.parent.removeChild(this.loadPage.obj); } this.loadPage = undefined; } }; log("[ezy.js] Welcome to the world of Ezy.js framework!");