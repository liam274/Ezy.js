`-` is unfinished
`+` is finished

- listener list dynamic control
- nested variable
+ complete plugin system
+ forEach attribute that can loops dict in varage, handing out key and value
- bind element with function in list, so change list only on re-rendering, without mutiple listeners
+ loading page & error page pre-render
+ attribute inherit
+ build-in value vaildation function
- condition parse str: {{if user}}...{{else}}...{{/if}}
- debug: true as a mode
- onCreated, onMounted, onUpdated, onDestroyed
- diff algorithm
- lazy loading, only load of which can be seen
- onBeforeRender, onAfterRender, onError, onStateChange
- offical plugins, for example routin plugin, animation plugin and form config plugin
- plugin market
- DevTools extension in browser
- CLI tools
- responsive playground online for customers to try
+ classify list, to let it become reusable
- `###` RESPONSIVE
+ expire object
+ main func
+ pipes
+ filter & filter argument