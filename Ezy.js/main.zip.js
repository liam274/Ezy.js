"use strict"; import *as utils from "./utils.js"; import *as storage from "./storage.js"; import *as cssComputer from "./cssComputer.js"; import { dangerousAttribute, isSafeURL } from "./safety.js"; export * from "./utils.js"; export * from "./storage.js"; export * from "./history.js"; let _allowClear = true; export function allowClear(clr) { _allowClear = clr } export const log = console.log, $ = document.querySelector.bind(document), $$ = document.createElement.bind(document), error = console.error.bind(console), warn = console.warn.bind(console), oriclear = console.clear.bind(console), clear = function (...args) { if (_allowClear) { oriclear(...args) } }; export const keyword = new Set(["type", "component", "tag", "events", "style", "varAs", "listener", "times", "title", "if", "content", "inherit", "validate", "expire", "text", "forEach", "innerHTML", "config", "data", "belt", "isFragment", "evaluate", "_type"]), errors = Object.freeze({ SECURITY_ERROR: -1, STRUCTURE_ERROR: 1, ASVAR_REWRITE_ERROR: 2, FORMAT_ERROR: 3, TEMPLATE_STRUCTURE_ERROR: 4, VALUE_ERROR: 5, EVAL_ERROR: 6, CLASSIFY_ERROR: 7, PIPE_ERROR: 8, RENDER_ERROR: 9, PHARSING_ERROR: 10, ID_ERROR: 11, TYPE_ERROR: 12, VARIABLE_ERROR: 13 }); export const dictionary = Object.freeze({ time: ["i", "index", "renderIndex"], key: ["key"], item: ["item", "value"] }); export const body = document.body, head = document.head; export const Ezy = Object.freeze({ plugins: [], add(plugin) { this.plugins.push(plugin) }, validates: { isInt(data) { return Number.isInteger(Number(data)) }, isAlphabet(data) { return utils.ALPHABET_REGEX.test(data) }, isEmail(data) { return utils.EMAIL_REGEX.test(data) }, isDate(data) { return utils.DATE_REGEX.test(data) }, maxl(data, len) { return data.length <= len }, minl(data, len) { return data.length >= len }, max(data, limit) { return data <= limit }, min(data, limit) { return data >= limit }, }, validateComponentIf(obj, varage, item, traceback) { if (item) { if (!varage.has(item)) { obj.set(errors.VALUE_ERROR); return Ezy.formatError(`render.#varage[component.if]not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error") } if (typeof varage.get(item) !== "function") { obj.set(errors.TYPE_ERROR); return Ezy.formatError(`expected render.#varage[component.if]as function,found ${typeof varage[item]},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } return !varage.get(item)() } }, validateValidation(obj, el, validate, traceback, parentNode) { const { rules, required } = validate; if (rules) { if (!Array.isArray(rules)) { obj.set(errors.TYPE_ERROR); return Ezy.formatError(`Error when rendering,expected component.validate as an array,found ${typeof rules},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } if (required) { parentNode.addEventListener("submit", async function (e) { e.preventDefault(); let a = 0; for (const val of rules) { const [vali, ...parms] = val.split(":"); if (Ezy.validates[vali]) { const state = await Ezy.validates[vali](el.value, ...parms); if (!state) { validate?.onCaught(); a = 1; break } } else { obj.set(errors.RENDER_ERROR); return Ezy.formatError(`Error when rendering,Ezy[component.validate]not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error") } } if (a === 0 && parentNode.checkValidity()) { parentNode.submit() } }) } el.addEventListener("input", async function () { let r = true; for (const val of rules) { const [vali, ...parms] = val.split(":"); if (Ezy.validates[vali]) { const state = await Ezy.validates[vali](el.value, ...parms); r = r && state } else { obj.set(errors.RENDER_ERROR); return Ezy.formatError(`Error when rendering,Ezy[component.validate]not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error") } } if (r) { el.classList.add("valid"); el.classList.remove("invalid"); validate?.onValid() } else { el.classList.remove("valid"); el.classList.add("invalid"); validate?.onInvalid() } }) } }, flat(data) { const result = []; for (const i of data) { if (Array.isArray(i)) { result.push(...this.flat(i)) } else { result.push(i) } } return result }, dialog: { alert(data) { const barrier = $$("div"); barrier.classList.add("alert-barrier"); const back = $$("div"); back.classList.add("alert-back"); barrier.appendChild(back); const title = $$("div"); title.classList.add("alert-title"); title.innerHTML = data.title; back.appendChild(title); const content = $$("div"); content.innerHTML = data.content; content.classList.add("alert-content"); back.appendChild(content); const confirm = $$("button"); confirm.innerHTML = "OK"; confirm.classList.add("alert-button"); confirm.addEventListener("click", () => { utils.removeChild(barrier); barrier.remove() }); back.appendChild(confirm); body.appendChild(barrier) }, confirm(data) { const barrier = $$("div"); barrier.classList.add("alert-barrier"); const back = $$("div"); back.classList.add("alert-back"); barrier.appendChild(back); const title = $$("div"); title.classList.add("alert-title"); title.innerHTML = data.title; back.appendChild(title); const content = $$("div"); content.innerHTML = data.content; content.classList.add("alert-content"); back.appendChild(content); const backk = $$("div"), confirm = $$("button"), cancel = $$("button"); backk.classList.add("confirm-btns-container"); confirm.innerHTML = "OK"; confirm.classList.add("alert-button"); confirm.addEventListener("click", () => { utils.removeChild(barrier); barrier.remove(); data.func(true, ...(data.props || [])) }); backk.appendChild(confirm); cancel.innerHTML = "Cancel"; cancel.classList.add("alert-button"); cancel.addEventListener("click", () => { utils.removeChild(barrier); barrier.remove(); data.func(false, ...(data.props || [])) }); backk.appendChild(cancel); back.appendChild(backk); body.appendChild(barrier) }, input(data) { const barrier = $$("div"); barrier.classList.add("alert-barrier"); const back = $$("div"); back.classList.add("alert-back"); barrier.appendChild(back); const title = $$("div"); title.classList.add("alert-title"); title.innerHTML = data.title; back.appendChild(title); const content = $$("div"); content.innerHTML = data.content; content.classList.add("password-content"); back.appendChild(content); const input = $$("input"); input.placeholder = data.placeholder; input.classList.add("alert-input"); input.addEventListener("keydown", (e) => { if (e.key === "Enter") { utils.removeChild(barrier); barrier.remove(); data.func(true, input.value, ...(data.props || [])) } }); content.appendChild(input); const backk = $$("div"), confirm = $$("button"), cancel = $$("button"); backk.classList.add("confirm-btns-container"); confirm.innerHTML = "OK"; confirm.classList.add("alert-button"); confirm.addEventListener("click", () => { utils.removeChild(barrier); barrier.remove(); data.func(true, input.value, ...(data.props || [])) }); backk.appendChild(confirm); cancel.innerHTML = "Cancel"; cancel.classList.add("alert-button"); cancel.addEventListener("click", () => { utils.removeChild(barrier); barrier.remove(); data.func(false, input.value, ...(data.props || [])) }); backk.appendChild(cancel); back.appendChild(backk); body.appendChild(barrier); input.focus() }, password(data) { const barrier = $$("div"); barrier.classList.add("alert-barrier"); const back = $$("div"); back.classList.add("alert-back"); barrier.appendChild(back); const title = $$("div"); title.classList.add("alert-title"); title.innerHTML = data.title; back.appendChild(title); const content = $$("div"); content.innerHTML = data.content; content.classList.add("password-content"); back.appendChild(content); const { input, bind, deletor } = utils.passworder(data); input.addEventListener("keydown", (e) => { if (e.key === "Enter") { utils.removeChild(barrier); barrier.remove(); data.func(true, bind(), ...(data.props || [])); deletor() } }); input.classList.add("alert-input"); content.appendChild(input); const backk = $$("div"), confirm = $$("button"), cancel = $$("button"); backk.classList.add("confirm-btns-container"); confirm.innerHTML = "OK"; confirm.classList.add("alert-button"); confirm.addEventListener("click", () => { utils.removeChild(barrier); barrier.remove(); data.func(true, bind(), ...(data.props || [])); deletor() }); backk.appendChild(confirm); cancel.innerHTML = "Cancel"; cancel.classList.add("alert-button"); cancel.addEventListener("click", () => { utils.removeChild(barrier); barrier.remove(); data.func(false, bind(), ...(data.props || [])); deletor() }); backk.appendChild(cancel); back.appendChild(backk); body.appendChild(barrier); input.focus() }, }, formatError(message, level, _error, die = true) { error(`[ezy.js]${Ezy.errors[level]}:${_error.toUpperCase()}:${message}`); if (die) { throw new Error(`[ezy.js]${Ezy.errors[level]}:${_error.toUpperCase()}:${message}`) } }, component(name, def) { if (typeof name !== "string") { throw new Error(`[ezy.js]CRITICAL ERROR:Value Error:Expected Ezy.component(name:string,def:Object),found name as ${typeof name}`) } if (typeof def !== "object") { throw new Error(`[ezy.js]CRITICAL ERROR:Value Error:Expected Ezy.component(name:string,def:Object),found def as ${typeof def}`) } this.components.set(name, def) }, components: new Map(), watchout(data, set) { return new Proxy(data, { set(target, key, value) { set.early?.(target, key, value); target[key] = value; set.late?.(target, key, value); return true }, get(target, prop) { const value = target[prop]; if (typeof value === "function") { return value.bind(target) } return value } }) }, errors: Object.freeze(["MINOR ERROR", "MAJOR ERROR", "CRITICAL ERROR"]), navigate(href) { if (!isSafeURL(href, this.whitelistProtocol)) { location.href = this.saviour; throw new Error(`Error when navigating,dangerous URL detected:${href}`) } let full = true; for (const guard of routeGuard.guards) { const result = guard(href); if (!result.allow) { if (result.href) { location.href = result.href; return } full = false } } if (full) { location.href = href } }, whitelistProtocol: [], saviour: "saviour.html" }); export const errorLevels = Object.freeze({ CRITICAL_ERROR: 2, MAJOR_ERROR: 1, MINOR_ERROR: 0 }); export const routeGuard = { builtin: new Set([]), guards: [] }; routeGuard.guards.push(function (href) { return { allow: routeGuard.builtin.has(href) } }); const csp = $$("meta"); head.appendChild(csp); csp.setAttribute("http-equiv", "Content-Security-Policy"); csp.setAttribute("content", "default-src 'self'; script-src 'self' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; worker-src 'self'; connect-src 'self'; font-src 'self';"); export const varage = {}; const vars = new Set(); export const MAXWAIT = 60000, HTTP_NOT_FOUND = 404, HTTP_TIMEOUT = 408; export const store = new storage.store(); export class render { #varage = new Map(); #oldBoys = new Map(); #typeExtend = new Map(); #listen2 = new Map(); #debug = false; #pluginLeftovers = { timeouts: [], events: [], animationFrames: [] }; #confirmer = undefined; #reporter = undefined; #cssBefore = new Map(); #cssAfter = new Set(); #style = $$("style"); #remarkableStyle = $$("style"); #myvars = new Set(); constructor(el, data, maxWait = MAXWAIT, namespace = {}) { head.appendChild(this.#style); head.appendChild(this.#remarkableStyle); this.maxWait = maxWait; this.data = data; this.loadPage = []; if (!data) { this.set(errors.STRUCTURE_ERROR); this.loadPage.push(this.loadingPage({ message: "Data structure missing.", level: errorLevels.CRITICAL_ERROR, _error: "Structure Error" }, HTTP_NOT_FOUND, this.maxWait)); return this } this.classify = new Map(Object.entries(data.classify || {})); if (!data.component) { this.set(errors.STRUCTURE_ERROR); this.loadPage.push(this.loadingPage({ message: "Data structure attribute \"component\" missing.", level: errorLevels.CRITICAL_ERROR, _error: "Structure Error" }, HTTP_NOT_FOUND, this.maxWait, "Resource page.data.component not found")); return this } this.namespace = namespace; if (typeof el === "string") { this.mainEl = $(el) } else { this.mainEl = el } this.original = this.mainEl.innerHTML; this.reload(); if (this.statusCode !== 0) { return this } } reload() { this.config = this.data.config || {}; this.config.escapeHTML = utils._default(this.config.escapeHTML, true); this.config.whitelistProtocol = Array.isArray(this.config.whitelistProtocol) ? this.config.whitelistProtocol : []; if (this.config.debug) { this.#debug = true } else { this.#debug = false } if (!this.#debug) { clear() } if (this.loadPage.length) { for (const i of this.loadPage) { this.clearLoading(i) } this.loadPage.length = 0 } for (const i of this.#pluginLeftovers.timeouts) { clearTimeout(i) } for (const i of this.#pluginLeftovers.events) { document.removeEventListener(...i) } for (const i of this.#pluginLeftovers.animationFrames) { cancelAnimationFrame(i) } if (this.config.urlFilter) { const { urlFilter } = this.config, { confirmer, reporter, rules } = urlFilter; if (!this.#confirmer && typeof confirmer === "function") { this.#confirmer = confirmer } if (!this.#confirmer) { this.set(errors.SECURITY_ERROR); urlFilter.onError?.(); return Ezy.formatError("Error when trying to setup URL filter, since data.config.urlFilter.confirmer is not a function, due to security concerns, we disallowed the process.", errorLevels.CRITICAL_ERROR, "Security Error") } if (!this.#reporter && typeof reporter === "function") { this.#reporter = reporter } if (!this.#reporter) { urlFilter.onError?.(); this.set(errors.SECURITY_ERROR); return Ezy.formatError("Error when trying to setup URL filter, since data.config.urlFilter.reporter is not a function, due to security concerns, we disallowed the process.", errorLevels.CRITICAL_ERROR, "Security Error") } if (!navigator.serviceWorker) { urlFilter.onError?.(); this.set(errors.SECURITY_ERROR); return Ezy.formatError("Error when trying to setup URL filter, since your browser doesn't support serviceWorker, we cannot provide any service.", errorLevels.CRITICAL_ERROR, "Security Error") } if (Array.isArray(rules)) { if (this.#confirmer(rules) !== true) { this.set(errors.SECURITY_ERROR); this.#reporter(); return Ezy.formatError("Error when trying to setup URL filter, URL WHITELIST HAS BEEN TAMPERED.", errorLevels.CRITICAL_ERROR, "Security Error") } navigator.serviceWorker.register(new URL("./firewall.js", import.meta.url)).then((function () { if (this.#debug) { log("[ezy.js] URL filter registered successful.") } return navigator.serviceWorker.ready }).bind(this)).then(reg => { if (navigator.serviceWorker.controller) { navigator.serviceWorker.controller.postMessage({ type: "UPDATE_RULES", rules: rules }) } else if (reg.active) { reg.active.postMessage({ type: "UPDATE_RULES", rules: rules }) } }) } else { urlFilter.onError?.(); this.set(errors.TYPE_ERROR); return Ezy.formatError(`Expected data.config.urlFilter.urls as array,found ${typeof rules}`, errorLevels.CRITICAL_ERROR, "Type Error") } } else { this.set(errors.SECURITY_ERROR); return Ezy.formatError("Error when trying to setup page, config.urlFilter not found", errorLevels.CRITICAL_ERROR, "Security Error") } this.historyRender = +new Date(); if (!this.data) { this.loadPage.push(this.loadingPage({ message: "Data structure missing.", level: errorLevels.CRITICAL_ERROR, _error: "Structure Error" }, HTTP_NOT_FOUND, this.maxWait)); return this.set(errors.STRUCTURE_ERROR) } this.loadPage.push(this.loadingPage({ message: "", level: errorLevels.CRITICAL_ERROR, _error: "Timeout Error" }, HTTP_TIMEOUT, this.maxWait, "Page render timeout")); this.clear(); this.#varage = new Map(Object.entries({ ...varage, ...(this.data.data || {}) })); this.statusCode = 0; this.systemPlot = { time: 0 }; if (this.config.style) { const c = []; for (const j in this.config.style) { const val = this.config.style[j], d = []; for (const i in val) { d.push(`${utils.camel2array(i).join("-")}:${val[i]};`) } c.push(`${j}{${d.join("")}}`) } this.#remarkableStyle.innerHTML = c.join("") } if (this.config.typeExtend) { for (const i in this.config.typeExtend) { const val = this.config.typeExtend[i]; if (!Array.isArray(val)) { this.set(errors.TYPE_ERROR); return Ezy.formatError(`Expected Array,found ${typeof val}.`, errorLevels.CRITICAL_ERROR, "Type Error") } this.#typeExtend.set(i, [...val]) } } this.render(this.data, this.mainEl); if (this.#debug) { log(`[ezy.js]Debug Message::Render consumed ${new Date() - this.historyRender}ms`) } } render(data, root, options = {}) { if (!data) { this.loadPage.push(this.loadingPage({ message: "Data structure missing.", level: errorLevels.CRITICAL_ERROR, _error: "Structure Error" }, HTTP_NOT_FOUND, this.maxWait, "Resource page.data not found", root)); return this.set(errors.STRUCTURE_ERROR) } this.statusCode = 0; if (data.onStart) { this.preRender(data.onStart) } else if (this.#debug) { warn("[ezy.js] MAJOR SUGGESTION: : Suggest adding onStart function list to handle preprocess") } for (const i of Ezy.plugins) { const { timeouts, events, animationFrames } = i.onStart?.(data) || {}; this.#pluginLeftovers.timeouts.push(...timeouts); this.#pluginLeftovers.events.push(...events); this.#pluginLeftovers.animationFrames.push(...animationFrames) } const el = document.createDocumentFragment(); this.sectionRender(data, el, data.name || "", data.title || "", this.contentRender, root, options); this.cssPutter(); if (this.#debug) { log(`%c[ezy.js]Render Program exits ${this.statusCode === 0 ? "" : "un"}successfully.Status Code:${this.statusCode}`, this.statusCode === 0 ? "font-size: 30px; font-weight: bold;color: #e0e0e0;" : "font-size: 30px; font-weight: bold;color: red;") } if (data.onLoad) { for (const i of data.onLoad) { i(data) } } else if (this.#debug) { warn("[ezy.js] MINOR SUGGESTION: : Suggest adding onLoad function list to handle onLoad process") } for (const i of Ezy.plugins) { const { timeouts, events, animationFrames } = i.onLoad?.(data) || {}; this.#pluginLeftovers.timeouts.push(...timeouts); this.#pluginLeftovers.events.push(...events); this.#pluginLeftovers.animationFrames.push(...animationFrames) } if (this.statusCode !== 0) { return } if (el) { root.appendChild(el) } if (this.loadPage.length) { for (const i of this.loadPage) { this.clearLoading(i) } this.loadPage.length = 0 } } edit(key, data) { if (this.#varage.get(key) === data) { return false } this.#varage.set(key, data); this.#oldBoys.clear(); if (this.#listen2.has(key)) { for (const val of [...this.#listen2.get(key)]) { const [obj, el, cleanup, options] = val; if (cleanup) { if (utils._default(options.deep, true)) { cleanup.innerHTML = "" } else { for (const node of [...cleanup.childNodes]) { if (node.nodeType === Node.TEXT_NODE) { node.remove() } } } this.render(obj, cleanup, options) } else { if (utils._default(options.deep, true)) { el.innerHTML = "" } else { for (const node of [...el.childNodes]) { if (node.nodeType === Node.TEXT_NODE) { node.remove() } } } this.render(obj, el, options) } } } return true } read(key) { if (this.#varage.has(key)) { return this.#varage.get(key) } else { this.set(errors.VARIABLE_ERROR); return Ezy.formatError(`Variable Error:Variable"${key}"not found`, errorLevels.CRITICAL_ERROR, "Variable Error") } } preRender(data) { for (const i of data.funcs) { i(this.data) } } sectionRender = (sectionData, parentElement, sectionName, title, createElement, root, options) => { const traceback = `${title}->${sectionName}`; if (!sectionData) { this.set(errors.TYPE_ERROR); return Ezy.formatError(`function found first parameter in ${sectionData},expected object,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } this.systemPlot.time = 0; for (const i in sectionData.component) { const item = sectionData.component[i]; if (Ezy.validateComponentIf(this, this.#varage, item.if, traceback)) { continue } if (this.statusCode !== 0) { return } const el = createElement(i, item, { ...(sectionData.config || {}), ...(i.config || {}) }, sectionData, parentElement, root, options); if (this.statusCode !== 0) { return } if (!(el instanceof Node)) { this.set(errors.VALUE_ERROR); return Ezy.formatError(`argument-function"createElement"return unexpected value,expected Node(or NodeLike object),in page ${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error") } parentElement.appendChild(el); this.systemPlot.time++ } }; #extendType(...data) { const result = []; for (const i of data) { result.push(...(this.#typeExtend.get(i) || [i])) } return result } #logic1(card, i, fatherData, fatherElement, first, replacement, traceback, config, root) { const classes = [...(i.type || []), ...(config.type || [])]; if (classes.some((char) => char.includes(" "))) { this.set(errors.VALUE_ERROR); return Ezy.formatError(`Error when computing classList,found" "char in className,${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error") } card.classList.add(...classes); if (!this.config.noComputeCSS) { this.putCSS(cssComputer.cssCompiler(this.#extendType(...classes))) } if (i.expire) { setTimeout((function () { card.innerHTML = ""; card.remove(); setTimeout(() => { i.expire.expired?.() }) }).bind(this), i.expire.date - this.historyRender) } Ezy.validateValidation(this, card, i.validate || "", traceback, fatherElement); if (this.statusCode !== 0) { return } if (i.data) { for (const k in i.data) { card.setAttribute(`data-${utils.camel2array(k).join("-")}`, this.preCompileStr(i.data[k], traceback, replacement)); if (this.statusCode !== 0) { return } } } this.beforePlugComponent(card, traceback); if (this.statusCode !== 0) { return } if (i.belt && first === 0) { const { buckle, reverseBuckle } = i.belt; if (buckle) { if (!Array.isArray(buckle)) { this.set(errors.TYPE_ERROR); return Ezy.formatError(`Expected component.belt.buckle as string[],found ${typeof buckle},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } for (const _buckle of buckle) { if (this.#varage.has(_buckle)) { this.#listen2.getOrInsert(_buckle, []).push([fatherData, card, root, i.belt.options || {}]) } else { this.set(errors.VARIABLE_ERROR); return Ezy.formatError(`varage variable ${_buckle}not found`, errorLevels.CRITICAL_ERROR, "Variable Error") } } } if (reverseBuckle) { if (typeof reverseBuckle !== "string") { this.set(errors.TYPE_ERROR); return Ezy.formatError(`Expected component.belt.reverseBuckle as string,found ${typeof reverseBuckle},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } if (this.#varage.has(reverseBuckle)) { card.addEventListener("input", event => { this.edit(reverseBuckle, event.target.value) }) } else { this.set(errors.VARIABLE_ERROR); return Ezy.formatError(`varage variable ${reverseBuckle}not found`, errorLevels.CRITICAL_ERROR, "Variable Error") } } } if (first === 0 && i.varAs) { this.asVar(card, i.varAs, traceback); if (this.statusCode !== 0) { return } } if (i.text) { card.title = this.preCompileStr(i.text, traceback, replacement) } if (i._type) { card.type = i._type } } #logic2(card, i, config, replacement, traceback) { utils.applyStyles(card, i.style); for (const j in (i.events || {})) { this.addListener(j, i, card, traceback); if (this.statusCode !== 0) { return } } let r = this.preCompileStr((i.content || ""), traceback, replacement); if (this.statusCode !== 0) { return } if (this.config.escapeHTML || config.escapeHTML || i.config?.escapeHTML) { r = utils.htmlEscape(r) } card.innerHTML = r; for (const j in i) { if (keyword.has(j)) { continue } card.setAttribute(j, this.preCompileStr(i[j], traceback, replacement)); if (this.statusCode !== 0) { return } } let ok = true; for (const danger of dangerousAttribute) { if (!isSafeURL(card.getAttribute(danger), this.config.whitelistProtocol)) { this.set(errors.SECURITY_ERROR); Ezy.formatError(`Found JavaScript injection in attribute"${danger}"as"${card.getAttribute(danger)}"when scanning attributes,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Security Error", false); ok = false } } if (!ok) { throw new Error("Found JavaScript injection when scanning attributes, rendering process interrupted."); } } contentRender = (_, i, config, fatherData, fatherElement, root, options) => { options.deep = utils._default(options.deep, true); const title = fatherData.title || "", traceback = `${title}`; if (typeof i === "string") { if (!(this.classify.has(i) || Ezy.components.has(i))) { this.set(errors.CLASSIFY_ERROR); return Ezy.formatError(`Error when trying to use classify component"${i}"without definition,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Classify Error") } i = this.classify.get(i) || Ezy.components.get(i) } const todo = document.createDocumentFragment(), frag = i.isFragment || false; if (i.async) { const { loader, setter, placeholder, error } = i.async; loader().then((data) => setter(data)).catch((err) => { const temp = { ...fatherData }; for (const key in error) { i[key] = error[key] } temp.inherit = temp.inherit || {}; temp.inherit.err = err; this.render(temp, fatherElement) }); for (const key in placeholder) { i[key] = placeholder[key] } } if (i.evaluate && this.#varage.has(i.evaluate)) { const obj = JSON.parse(this.#varage.get(i.evaluate)); for (const key in obj) { i[key] = obj[key] } } if (i.forEach) { if (!this.#varage.has(i.forEach)) { this.set(errors.RENDER_ERROR); return Ezy.formatError(`Error when rendering,expected forEach variable,not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error") } const obj = this.#varage.get(i.forEach); if (!(obj && typeof obj === "object")) { this.set(errors.RENDER_ERROR); return Ezy.formatError(`Error when rendering,expected object as forEach variable value,found ${obj},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error") } let first = -1; for (const k in obj) { first++; const card = (frag ? document.createDocumentFragment() : $$(i.tag || config.tag || "div")), replacement = { ...this.systemPlot, ...(i.inherit || {}), key: k, item: obj[k] }; if (!frag) { this.#logic1(card, i, fatherData, fatherElement, first, replacement, traceback, config, root); if (this.statusCode !== 0) { return } } todo.appendChild(card); this.plugComponent(card, traceback); if (this.statusCode !== 0) { return } if (!frag) { this.#logic2(card, i, config, replacement, traceback); if (this.statusCode !== 0) { return } } if (options.deep) { this.pushComponent(i, utils.isDocumentFragment(card) ? fatherElement : card, traceback, { ...config, ...(i.config || {}) }, replacement) } if (this.statusCode !== 0) { return } } } else { for (let k = 0; k < (i.times || 1); k++) { const card = (frag ? document.createDocumentFragment() : $$(i.tag || config.tag || "div")); if (!frag) { this.#logic1(card, i, fatherData, fatherElement, k, i.inherit || {}, traceback, config, root); if (this.statusCode !== 0) { return } } todo.appendChild(card); this.plugComponent(card, traceback); if (this.statusCode !== 0) { return } if (!frag) { this.#logic2(card, i, config, i.inherit || {}, traceback); if (this.statusCode !== 0) { return } } if (options.deep) { this.pushComponent(i, utils.isDocumentFragment(card) ? fatherElement : card, traceback, { ...config, ...(i.config || {}) }, i.inherit) } if (this.statusCode !== 0) { return } } } return todo }; set(code) { this.statusCode = code } evaluateExpression(expr, traceback, extraScope) { expr = expr.trim(); if (!expr) { return "" } let last = false, double = false, exist = false; for (const i of expr) { if (i === "|") { exist = true; if (last) { double = true; break } last = true; continue } last = false; double = false } if (!double && exist) { const varName = []; let first = true, result = undefined; for (const char of expr) { if (char === "|") { let name = varName.join("").trim(); if (first) { if (extraScope.has(name)) { result = extraScope.get(name) } else { this.set(errors.PHARSING_ERROR); return Ezy.formatError(`Error when trying to access variable ${name},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error") } first = false } else { name = name.split(":"); if (extraScope.has(name[0])) { if (typeof extraScope.get(name[0]) === "function") { result = this.evaluateExpression(`${name[0]}(result,${name.filter((_, i) => i).join(", ")})`, traceback, { ...Object.fromEntries(extraScope), result }) } else { this.set(errors.PHARSING_ERROR); return Ezy.formatError(`Error when parsing,expected filter as function,found ${typeof extraScope.get(name[0])},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error") } } else { this.set(errors.PHARSING_ERROR); return Ezy.formatError(`Error when trying to access variable ${name[0]},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error") } } varName.length = 0 } else { varName.push(char) } } let name = varName.join("").trim(); if (first) { if (extraScope.has(name)) { result = extraScope.get(name) } else { this.set(errors.PHARSING_ERROR); return Ezy.formatError(`Error when trying to access variable ${name},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error") } first = false } else { name = name.split(":"); if (extraScope.has(name[0])) { if (typeof extraScope.get(name[0]) === "function") { result = this.evaluateExpression(`${name[0]}(result,${name.filter((_, i) => i).join(", ")})`, traceback, { ...Object.fromEntries(extraScope), result }) } else { this.set(errors.PHARSING_ERROR); return Ezy.formatError(`Error when parsing,expected filter as function,found ${typeof extraScope.get(name[0])},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error") } } else { this.set(errors.PHARSING_ERROR); return Ezy.formatError(`Error when trying to access variable ${name[0]},not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Parse Error") } } return result } const keys = Object.keys(Object.fromEntries(extraScope)); const values = keys.map(k => extraScope.get(k)); try { const fn = new Function(...keys, `return(${expr})`); const result = fn(...values); return result === undefined ? "" : String(result) } catch (e) { if (e instanceof ReferenceError) { if (this.#debug) { warn(`[ezy.js]Warning:Variable not defined in"${expr}"at ${traceback}`) } return "" } error(e); this.set(errors.EVAL_ERROR); Ezy.formatError(`Failed to evaluate"${expr}"in ${traceback}`, errorLevels.CRITICAL_ERROR, "Eval Error"); return "" } } #setOldBoys(name, val, bool) { if (bool) { this.#oldBoys.set(name, val) } else { return val } } preCompileStr(data, traceback, replacement = {}) { if (typeof data !== "string") { this.set(errors.TYPE_ERROR); return Ezy.formatError(`Error when trying to parse string,expected string,found ${data}`, errorLevels.CRITICAL_ERROR, "Type Error") } const result = []; let skip = false, startVar = false, longVar = false, stop = false, doubleStop = false, varName = []; replacement = { ...this.systemPlot, ...replacement }; const newReplacement = new Map(), _varage = this.#varage, local = new Map(); for (const i in replacement) { for (const t of (dictionary[i] || [i])) { newReplacement.set(t, replacement[i]) } } for (const [i, val] of newReplacement) { data = data.replaceAll(`{{${i}}}`, val) } for (const i of data) { if (skip) { if (varName.length) { varName.push(i) } else { result.push(i) } skip = false; continue } if ((stop && !longVar) || (doubleStop && longVar)) { varName = varName.join("").trim(); const _var = stop ? _varage : newReplacement; if (this.#oldBoys.has(varName)) { result.push(this.#oldBoys.get(varName)) } else if (varName in local) { result.push(local[varName]) } else if (_var.has(varName)) { if ((typeof _var.get(varName)) === "function") { const temp = String(_var.get(varName)()); result.push(temp); local[varName] = this.#setOldBoys(varName, temp, stop) || temp } else { const temp = String(_var.get(varName)); result.push(temp); local[varName] = this.#setOldBoys(varName, temp, stop) || temp } } else { try { const temp = String(this.evaluateExpression(varName, traceback, _var)); result.push(temp); local[varName] = this.#setOldBoys(varName, temp, stop) || temp } catch (e) { this.set(errors.EVAL_ERROR); Ezy.formatError(`Error when trying to eval expression ${varName},as below,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Eval Error"); return error(e) } } stop = false; doubleStop = false; startVar = false; longVar = false; varName = [] } if (i === "\\") { skip = true; continue } if (i === "{") { if (varName.length) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,unexpected character"{"within value,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } if (longVar) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,unexpected character"{"with two openings already,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } if (startVar) { startVar = false; longVar = true } else { startVar = true } continue } if (i === "}") { if (!(startVar || longVar)) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,unexpected ending without any opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } if (stop) { if (startVar) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,unexpected double ending with single opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } stop = false; doubleStop = true; continue } if (startVar) { startVar = false; if (doubleStop) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,unexpected ending with single opening already,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } stop = true } if (longVar) { if (stop) { doubleStop = true; stop = false } else { stop = true } } continue } if (startVar || longVar) { varName.push(i); continue } result.push(i) } if (longVar && !doubleStop) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,unclosed double opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } if (startVar && !stop) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,unclosed single opening,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } if (skip) { this.set(errors.FORMAT_ERROR); return Ezy.formatError(`Error when formatting string,expected any character after\\in ${traceback}`, errorLevels.CRITICAL_ERROR, "Format Error") } varName = varName.join("").trim(); if (this.#oldBoys.has(varName)) { result.push(this.#oldBoys.get(varName)) } else if (varName in local) { result.push(local[varName]) } else if (stop || doubleStop) { const _var = stop ? _varage : newReplacement; if (_var.has(varName)) { if ((typeof _var.get(varName)) === "function") { const temp = String(_var.get(varName)()); result.push(temp); local[varName] = this.#setOldBoys(varName, temp, stop) || temp } else { const temp = String(_var.get(varName)); result.push(temp); local[varName] = this.#setOldBoys(varName, temp, stop) || temp } } else { try { result.push(String(this.evaluateExpression(varName, traceback, _var))) } catch (e) { this.set(errors.EVAL_ERROR); Ezy.formatError(`Error when trying to eval expression ${varName},as below,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Eval Error"); return error(e) } } } return result.join("") } #pushLogic(j, el, replace, config, traceback, myTraceback, first, parentNode, i) { for (const evt in j.events) { this.addListener(evt, j, el, myTraceback); if (this.statusCode !== 0) { return } } for (const parm in j) { if (keyword.has(parm)) { continue } el.setAttribute(parm, this.preCompileStr(j[parm], myTraceback, replace)); if (this.statusCode !== 0) { return } } let ok = true; for (const danger of dangerousAttribute) { if (!isSafeURL(el.getAttribute(danger), this.config.whitelistProtocol)) { this.set(errors.SECURITY_ERROR); Ezy.formatError(`Found JavaScript injection in attribute"${danger}"as"${el.getAttribute(danger)}"when scanning attributes,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Security Error", false); ok = false } } if (!ok) { throw new Error("Found JavaScript injection when scanning attributes, rendering process interrupted."); } let r = this.preCompileStr((j.content || ""), myTraceback, replace); if (this.statusCode !== 0) { return } if (this.config.escapeHTML || config.escapeHTML || j.config?.escapeHTML) { r = utils.htmlEscape(r) } el.innerHTML = r; if (j.expire) { setTimeout((function () { el.innerHTML = ""; el.remove(); setTimeout(() => { j.expire.expired?.() }) }).bind(this), j.expire.date - this.historyRender) } Ezy.validateValidation(this, el, j.validate || "", traceback, parentNode); if (this.statusCode !== 0) { return } if (j.text) { el.title = this.preCompileStr(j.text, myTraceback, replace) } if (j._type) { el.type = j._type } if (j.data) { for (const k in j.data) { el.setAttribute(`data-${utils.camel2array(k).join("-")}`, this.preCompileStr(j.data[k], myTraceback, replace)); if (this.statusCode !== 0) { return } } } this.beforePlugComponent(el, myTraceback); if (this.statusCode !== 0) { return } if (j.belt && first === 0) { const { buckle, reverseBuckle } = j.belt; if (buckle) { if (!Array.isArray(buckle)) { this.set(errors.TYPE_ERROR); return Ezy.formatError(`Expected component.belt.buckle as string[],found ${typeof buckle},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } for (const _buckle of buckle) { if (this.#varage.has(_buckle)) { this.#listen2.getOrInsert(_buckle, []).push([i, parentNode, undefined, j.belt.options || {}]) } else { this.set(errors.VARIABLE_ERROR); return Ezy.formatError(`varage variable ${_buckle}not found`, errorLevels.CRITICAL_ERROR, "Variable Error") } } } if (reverseBuckle) { if (typeof reverseBuckle !== "string") { this.set(errors.TYPE_ERROR); return Ezy.formatError(`Expected component.belt.reverseBuckle as string,found ${typeof reverseBuckle},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } if (this.#varage.has(reverseBuckle)) { el.addEventListener("input", event => { this.edit(reverseBuckle, event.target.value) }) } else { this.set(errors.VARIABLE_ERROR); return Ezy.formatError(`varage variable ${reverseBuckle}not found`, errorLevels.CRITICAL_ERROR, "Variable Error") } } } if (first === 0 && j.varAs) { this.asVar(el, j.varAs, myTraceback); if (this.statusCode !== 0) { return } } } pushComponent(i, parentNode, traceback, config = {}, replacement = {}) { const own = { time: 0 }, todo = document.createDocumentFragment(), frag = i.isFragment || false; for (let j of (i.component || [])) { if (typeof j === "string") { if (!(this.classify.has(j) || Ezy.components.has(j))) { this.set(errors.CLASSIFY_ERROR); return Ezy.formatError(`Error when trying to use classify component"${j}"without definition,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Classify Error") } j = this.classify.get(j) || Ezy.components.get(j) } if (Ezy.validateComponentIf(this, this.#varage, j.if, traceback)) { continue } if (this.statusCode !== 0) { return } if (j.async) { const { loader, setter, placeholder, error } = j.async; loader().then((data) => setter(data)).catch((err) => { const temp = { ...i }; for (const key in error) { j[key] = error[key] } temp.inherit = temp.inherit || {}; temp.inherit.err = err; this.render(temp, parentNode) }); for (const key in placeholder) { j[key] = placeholder[key] } } if (j.evaluate && this.#varage.has(j.evaluate)) { const obj = JSON.parse(this.#varage.get(j.evaluate)); for (const key in obj) { j[key] = obj[key] } } if (j.forEach) { if (!this.#varage.has(j.forEach)) { this.set(errors.RENDER_ERROR); return Ezy.formatError(`Error when rendering,expected forEach variable,not found,in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error") } const obj = this.#varage.get(j.forEach); if (!(obj && typeof obj === "object")) { this.set(errors.RENDER_ERROR); return Ezy.formatError(`Error when rendering,expected object as forEach variable value,found ${obj},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Render Error") } let first = -1; for (const k in obj) { first++; const el = (frag ? document.createDocumentFragment() : $$(j.tag || config.tag || j.config?.tag || "div")); if (!frag) { const classes = [...(j.type || []), ...(config.type || [])]; if (classes.some((char) => char.includes(" "))) { this.set(errors.VALUE_ERROR); return Ezy.formatError(`Error when computing classList,found" "char in className,${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error") } el.classList.add(...classes); if (!this.config.noComputeCSS) { this.putCSS(cssComputer.cssCompiler(this.#extendType(...classes))) } utils.applyStyles(el, j.style) } const myTraceback = frag ? traceback : (traceback + `->${el.tagName}${el.id ? "#" + el.id : ""}.${[...el.classList].join(".")}`), replace = { ...replacement, ...j.inherit, key: k, item: obj[k], ...own }; if (!frag) { this.#pushLogic(j, el, replace, config, traceback, myTraceback, first, parentNode, i); if (this.statusCode !== 0) { return } } todo.appendChild(el); this.plugComponent(el, myTraceback); if (this.statusCode !== 0) { return } this.pushComponent(j, utils.isDocumentFragment(el) ? parentNode : el, myTraceback, { ...config, ...(j.config || {}) }, replace); if (this.statusCode !== 0) { return } } } else { for (let k = 0; k < (j.times || 1); k++) { const el = (frag ? document.createDocumentFragment() : $$(j.tag || config.tag || j.config?.tag || "div")); if (!frag) { const classes = [...(j.type || []), ...(config.type || [])]; if (classes.some((char) => char.includes(" "))) { this.set(errors.VALUE_ERROR); return Ezy.formatError(`Error when computing classList,found" "char in className,${traceback}`, errorLevels.CRITICAL_ERROR, "Value Error") } el.classList.add(...classes); if (!this.config.noComputeCSS) { this.putCSS(cssComputer.cssCompiler(this.#extendType(...classes))) } utils.applyStyles(el, j.style) } const myTraceback = frag ? traceback : (traceback + `->${el.tagName}${el.id ? "#" + el.id : ""}.${[...el.classList].join(".")}`); if (!frag) { this.#pushLogic(j, el, { ...replacement, ...j.inherit, ...own }, config, traceback, myTraceback, k, parentNode, i); if (this.statusCode !== 0) { return } } todo.appendChild(el); this.plugComponent(el, myTraceback); if (this.statusCode !== 0) { return } this.pushComponent(j, utils.isDocumentFragment(el) ? parentNode : el, myTraceback, { ...config, ...(j.config || {}) }, { ...replacement, ...j.inherit, ...own }); if (this.statusCode !== 0) { return } } } own.time++; parentNode.appendChild(todo) } } asVar(el, varAs, traceback) { if (varAs) { if (vars.has(varAs)) { this.set(errors.ID_ERROR); return Ezy.formatError(`when rendering ${el.tagName}.${[...el.classList].join(".")},id collide to"${varAs}",in ${traceback}`, errorLevels.CRITICAL_ERROR, "ID Error") } vars.add(varAs); this.#myvars.add(varAs); el.id = varAs } } clear() { for (const i of vars) { window[i].id = "" } utils.removeChild(this.mainEl); this.mainEl.innerHTML = this.original; this.#remarkableStyle.remove(); this.#remarkableStyle = $$("style"); head.appendChild(this.#remarkableStyle); this.#oldBoys.clear(); this.#listen2.clear(); for (const i of this.#myvars) { vars.delete(i) } this.#myvars.clear() } plugComponent(el, traceback) { for (const i of Ezy.plugins) { const { timeouts, events, animationFrames } = i.onComponentLoad?.(this, el, traceback) || {}; this.#pluginLeftovers.timeouts.push(...timeouts); this.#pluginLeftovers.events.push(...events); this.#pluginLeftovers.animationFrames.push(...animationFrames); if (this.statusCode !== 0) { return } } } beforePlugComponent(el, traceback) { for (const i of Ezy.plugins) { const { timeouts, events, animationFrames } = i.beforeComponentLoad?.(this, el, traceback) || {}; this.#pluginLeftovers.timeouts.push(...timeouts); this.#pluginLeftovers.events.push(...events); this.#pluginLeftovers.animationFrames.push(...animationFrames); if (this.statusCode !== 0) { return } } } addListener(j, i, el, traceback) { const obj = i.events[j], { listener } = obj; if (!listener) { this.set(errors.VALUE_ERROR); return Ezy.formatError(`Expected"listener"attribute in second parameter->events[first parameter],in ${traceback}`, errorLevels.MINOR_ERROR, "Value Error") } if (!Array.isArray(listener)) { this.set(errors.TYPE_ERROR); return Ezy.formatError(`Expected"listener"attribute as funciton[],found ${typeof listener},in ${traceback}`, errorLevels.CRITICAL_ERROR, "Type Error") } if (obj.preventDefault) { el.addEventListener(utils.removePrefix(j.toLowerCase(), "on"), function (e) { e.preventDefault(); for (const i of listener) { i(e) } }) } else { el.addEventListener(utils.removePrefix(j.toLowerCase(), "on"), function (e) { for (const i of listener) { i(e) } }) } } loadingPage({ message, level, _error }, errorCode, guillotine = MAXWAIT, reason = "Resource page.data not found", parentNode = body) { const pot = $$("div"); this.putCSS(cssComputer.cssCompiler(["display-flex", "horizontal-mid", "vertical-mid", "bg-color-white"])); this.cssPutter(); pot.classList.add("display-flex", "horizontal-mid", "vertical-mid", "bg-color-white"); pot.style.width = "100%"; pot.style.height = parentNode === body ? "100vh" : "100%"; const temp = $$("img"); temp.src = "/assets/loading.svg"; pot.appendChild(temp); parentNode.appendChild(pot); return { obj: pot, id: setTimeout(() => { pot.remove(); this.errorPage({ message, level, _error }, errorCode, reason, parentNode).remove() }, guillotine) } } errorPage({ message, level, _error }, errorCode, reason, parentNode = body) { Ezy.formatError(message, level, _error, false); const pot = $$("div"); this.putCSS(cssComputer.cssCompiler(["display-flex", "bg-color-white", "horizontal-mid", "vertical-mid"])); this.cssPutter(); pot.classList.add("display-flex", "bg-color-white", "horizontal-mid", "vertical-mid"); pot.style.width = "100%"; pot.style.height = parentNode === body ? "100vh" : "100%"; const div = $$("div"); div.innerHTML = errorCode || "404"; div.style.fontSize = "20px"; div.style.border = "1px solid black"; div.style.borderWidth = "0 1px 0 0"; div.style.padding = "0 50px"; div.style.color = "black"; div.style.width = "auto"; div.style.height = "auto"; const another = $$("div"); another.innerHTML = reason; another.style.fontSize = "18px"; another.style.color = "black"; another.style.padding = "0 50px"; another.style.width = "auto"; another.style.height = "auto"; pot.appendChild(div); pot.appendChild(another); parentNode.appendChild(pot); return { remove() { pot.remove() } } } clearLoading({ id, obj }) { if (!(id && obj)) { return } clearTimeout(id); obj.remove() } varage() { return Object.fromEntries(this.#varage) } cssPutter() { const result = []; for (const [name, val] of this.#cssBefore) { const { key, value, theme } = val; if (this.#cssAfter.has(name)) { continue } result.push(cssComputer.themeSetter(theme, `.${CSS.escape(name)}{${cssComputer.specializeCSS(theme, key, value)}}`)); this.#cssAfter.add(name) } this.#style.innerHTML += result.join("") } putCSS(result) { for (const [key, val] of result) { this.#cssBefore.set(key, val) } } entireClean() { utils.removeChild(this.mainEl); this.#remarkableStyle.remove(); this.#style.remove() } setTheme(themes) { this.mainEl.setAttribute("data-theme", themes.join(" ")) } };